# Local config / secrets
.env
.env.lock
runtime/secrets/
runtime/generated/
runtime/backups/
runtime/addons/
*.bak-before-*

# Runtime state / generated service configs
runtime/game/
runtime/rabbitmq-admin/
runtime/rabbitmq-game/
runtime/postgres/
runtime/director/
runtime/server-gateway/
runtime/fake-k8s-serviceaccount/

# Work/output
work/
*.log
*.tmp
.DS_Store
console/api/node_modules/
console/web/node_modules/
# Any other node_modules, wherever it appears. A stray `npx vitest` run from
# the repo root creates node_modules/.vite/ here, and the two paths above did
# not cover it -- a cache artifact reached main that way (see #538).
node_modules/
console/web/dist/
*.tsbuildinfo
**/bin/
**/obj/

# Python/cache
__pycache__/
*.pyc
.security-reports/
runtime/local-static/
